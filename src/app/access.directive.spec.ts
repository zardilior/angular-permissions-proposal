import { AccessDirective } from './access.directive';

describe('AccessDirective', () => {
  it('should create an instance', () => {
    const directive = new AccessDirective();
    expect(directive).toBeTruthy();
  });
});
